import React from 'react';

import {Text} from 'react-native';

const FunctionalComponent = (props) => {
  return <Text>This is a, {props.componentType}</Text>;
};

export default FunctionalComponent;
